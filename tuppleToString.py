# Converting a tupple into a string

tup = ('S', 'o', 'u', 'm', 'e', 'n')

# Use the 'join' method to concatenate the tupple elements
str = ''.join(tup)

print("The converted form of the tupple is:",str)